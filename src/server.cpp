#include "server.h"
#include "sockpp/tcp_acceptor.h"

int HTTPServer::Serve(std::string path) {
    // nothing yet
    return 0;
}

int HTTPServer::Listen(int16_t port) {
    // nothing yet
    return 0;
}